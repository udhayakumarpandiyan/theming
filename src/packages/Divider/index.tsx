import React from 'react';
import MatDivider, { DividerProps } from '@material-ui/core/Divider';

const Divider = (props: DividerProps) => {
  return <MatDivider {...props}></MatDivider>;
};

export default Divider;
