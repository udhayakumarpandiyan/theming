import MatTextField, { TextFieldProps } from '@material-ui/core/TextField';
import React from 'react';

const TextField = (props: TextFieldProps) => {
  return <MatTextField {...props} />;
};

export default TextField;
